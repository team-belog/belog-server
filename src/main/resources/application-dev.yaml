spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: "${DB_URL}"
    username: "${DB_USERNAME}"
    password: "${DB_PASSWORD}"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

server:
  port: "${SERVER_PORT:8080}"
  forward-headers-strategy: framework

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

auth:
  jwt:
    refresh-cookie-secure: true
    refresh-cookie-same-site: None

app:
  cors:
    allowed-origins:
      - https://belog.co.kr
      - https://www.belog.co.kr
      - https://api-dev.belog.co.kr
    allowed-origin-patterns:
      - "http://localhost:*"
      - "http://127.0.0.1:*"
