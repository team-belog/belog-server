# 실행 프로필
SPRING_PROFILES_ACTIVE=local

# MySQL 접속 정보
DB_URL='jdbc:mysql://localhost:3306/belog'
DB_USERNAME=belog
DB_PASSWORD=belog-local-password

# 서버 설정
SERVER_PORT=8080

# CORS 허용 Origin (여러 값은 쉼표로 구분)
APP_CORS_ALLOWED_ORIGINS=http://localhost:3000
APP_CORS_ALLOWED_ORIGIN_PATTERNS=
